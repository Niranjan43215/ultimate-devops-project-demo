name: Code Quality Scan
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run BuildErrors
        run: docker run --rm -v ${{ github.workspace }}:/src gramener/builderrors
